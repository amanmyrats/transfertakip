import { DefaultExpenseType } from './default-expense-type';

describe('DefaultExpenseType', () => {
  it('should create an instance', () => {
    expect(new DefaultExpenseType()).toBeTruthy();
  });
});
