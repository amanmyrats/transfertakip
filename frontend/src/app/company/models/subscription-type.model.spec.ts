import { SubscriptionType } from './subscription-type.model';

describe('SubscriptionType', () => {
  it('should create an instance', () => {
    expect(new SubscriptionType()).toBeTruthy();
  });
});
